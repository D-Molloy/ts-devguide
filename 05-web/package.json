{
  "scripts": {
    "start:db": "json-server -w -p 4000 db.json",
    "start:parcel": "parcel index.html"
  },
  "devDependencies": {
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "axios": "^0.21.0"
  }
}
